[WepUpgradeFix.X2DownloadableContentInfo_WepUpgradeFix]


; Fields for FixAbility Entries:
; TemplateName: Template name of the ability to fix, duh!
; OnByDefault: Status of fix before MCM configuration, defauls to TRUE
; DisabledByMod: Will be force diabled (and greyed out in MCM) if the named mod is enabled
; AddPrimaryViz: Whether to explicitly add any visualization for ammo/weapon effects for the primary target, defaults to FALSE
; IgnoreExistingDelegates: If set ignores existing BuildNewGameStatFn/BuildVisualizationFn, and uses typical ability delegates instead. defaults to FALSE
; SetFlag: Whether to set the ammo effects and weapon effects flags to true, defaults to TRUE

;FixAbility is to add Ammo effects/etc to multitarget skills
;this is used to allow the immolator to burn stuff.
+FixAbility = (TemplateName="MZFireThrower")
+FixAbility = (TemplateName="MZFireThrowerOverwatchShot")
+FixAbility = (TemplateName="MZChemstormFire")
+FixAbility = (TemplateName="MZPressureBlast")
+FixAbility = (TemplateName="MZChemthrowerSuppression") ;suppresion applies bonus effects immediately, instead of waiting for the shot.
+FixAbility = (TemplateName="MZChemthrowerKillZoneShot")